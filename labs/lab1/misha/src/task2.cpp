#include "task2_openmp.h"

int main() {
    init(2.06, 4, true);

    // findMinFuel(180);

    // double start_time = omp_get_wtime();

    // analyzeAllAngles(220, 360, 5.0);

    // double end_time = omp_get_wtime();
    // std::cout << "Общее время выполнения: " << (end_time - start_time) << " секунд" << std::endl;
    // return 0;
}
